# Templates

Future home for template-specific overrides (custom section components,
theme tokens, animation presets). The renderer + registry are template-agnostic;
templates plug in by extending the `sectionRegistry` map.
